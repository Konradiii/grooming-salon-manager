# Copy to .env.production and set to your deployed backend's URL before
# running `npm run build` for a real deployment. Leave unset for local dev
# (falls back to the Vite dev server's /api proxy).
VITE_API_URL=https://your-backend.example.com/api
